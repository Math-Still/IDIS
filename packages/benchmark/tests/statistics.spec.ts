import{describe,expect,it}from'vitest';import{percentile,summarizeSamples}from'../src';
describe('benchmark statistics',()=>{it('computes deterministic percentiles',()=>{expect(percentile([1,2,3,4,5],.5)).toBe(3);expect(percentile([1,2,3,4,5],.95)).toBe(4.8);});it('keeps failures out of latency distribution',()=>{const s=summarizeSamples([{seq:1,success:true,latencyMs:10,startedAt:0,completedAt:10},{seq:2,success:false,latencyMs:null,startedAt:20,completedAt:30,error:'x'}]);expect(s.total).toBe(2);expect(s.success).toBe(1);expect(s.errors).toBe(1);expect(s.meanMs).toBe(10);});});
