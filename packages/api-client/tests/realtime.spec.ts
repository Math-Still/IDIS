import{describe,expect,it}from'vitest';import{DEFAULT_RECONNECT_POLICY}from'../src/realtime';describe('realtime defaults',()=>{it('uses bounded reconnect defaults',()=>{expect(DEFAULT_RECONNECT_POLICY.baseDelayMs).toBeGreaterThan(0);expect(DEFAULT_RECONNECT_POLICY.maxDelayMs).toBeGreaterThan(DEFAULT_RECONNECT_POLICY.baseDelayMs);expect(DEFAULT_RECONNECT_POLICY.jitterRatio).toBeGreaterThanOrEqual(0);expect(DEFAULT_RECONNECT_POLICY.jitterRatio).toBeLessThanOrEqual(1);});});